---
type: meta
title: "Lint Report — 2026-04-24"
created: 2026-04-24
updated: 2026-04-24
tags:
  - type/meta
  - meta/lint
status: mature
---

# Lint Report — 2026-04-24

> Generated by the `wiki-lint` skill. A proposal, not an edit queue —
> no finding is auto-fixed without explicit user confirmation.
>
> **Update 2026-04-24 19:45 — all 5 findings resolved.** See `## Resolution Pass` at the bottom.

## Summary

- Pages scanned: 76 (2 sources, 6 topics, 38 concepts, 27 entities, 3 meta) + 4 root meta (`index.md`, `hot.md`, `overview.md`, `log.md`)
- Findings at scan time: **0 CRITICAL**, **3 HIGH**, **0 MEDIUM**, **2 LOW**
- Findings outstanding: **0** (all resolved — see `## Resolution Pass`)
- Reliability signals aggregate: 0 contradictions, 0 supersessions, 48 pages carrying 1+ open `[!gap]` / `(?)` marker (all within 30-day grace window → not individually flagged)
- Dead wikilinks at scan time: 4 (1 real — `[[ByteNet]]`; 3 `.md`-suffix variants in `log.md`). Now 0.
- Orphan pages: 0
- Index drift: clean — all 73 content pages reachable from `index.md`; all `index.md` wikilinks resolve

## CRITICAL

*(none)*

## HIGH

### [source-status] `raw/manifest.yaml` does not track the Müller & Guido book — **stale at scan time**

Re-check at 19:45 shows `raw/manifest.yaml` already contains three active entries for the Müller & Guido book (Chapter 4 markdown, Chapter 3 PDF, and the MinerU extraction), all pointing at `wiki_page: Introduction to Machine Learning with Python (Muller & Guido)`. The manifest was updated between the scan and the re-check, so this finding was already resolved before the resolution pass ran. **No action taken.**

### [missing-sources-footer] 27 pages lack a literal `## Sources` heading — **resolved**

Split into two sub-fixes:

- **Variant heading (`## Evidence and Sources`, 13 concept pages) — already resolved before this report was written.** Re-check confirms all 13 concept pages ([[Attention Mechanism]], [[Beam Search]], [[BLEU Score]], [[Byte-Pair Encoding]], [[Encoder-Decoder Architecture]], [[Label Smoothing]], [[Layer Normalization]], [[Multi-Head Attention]], [[Positional Encoding]], [[Residual Connection]], [[Scaled Dot-Product Attention]], [[Self-Attention]], [[Transformer]]) end with `## Sources`, not `## Evidence and Sources`. Prior-session rename.
- **No sources heading at all (14 entity / source pages) — resolved in the 2026-04-24 resolution pass.** All 13 entity pages received a standard `## Sources` footer listing `[[Attention Is All You Need]]`. The source page itself ([[Attention Is All You Need]]) had its final `## Raw Source` section renamed to `## Sources`.

### [dead-links] `[[ByteNet]]` has no destination — **stale at scan time**

Re-check at 19:45 finds no `[[ByteNet]]` wikilink anywhere in the wiki. Four pages mention "ByteNet" as plain text in prose (not a wikilink) — these do not produce dead-link reports. The bracketed reference was removed in a prior session. **No action taken.**

## MEDIUM

*(none)*

Rules that produced no findings today:

- `stale-volatile`: no `time-sensitive` pages older than 60 days, no `evolving` pages older than 180 days.
- `unresolved-gap`: 48 pages carry `[!gap]` or `(?)` markers, but every one of them was `updated` within the last 7 days — below the 30-day reporting threshold.
- `stale-pages`: no page `updated` > 90 days.
- `stale-meta`: `hot.md` and `overview.md` both `updated: 2026-04-22` (2 days old, threshold is 7).
- `orphan-pages`: 0 — every content page has ≥ 1 inbound wikilink.
- `cascade-supersession`: no `[!superseded]` callouts anywhere in the wiki, so nothing to cascade.

## LOW

### [dead-links] `log.md` uses `[[index.md]]`, `[[hot.md]]`, `[[overview.md]]` with file extension — **resolved**

All three occurrences in the 2026-04-21 and 2026-04-22 entries rewritten to extensionless form: `[[index]]`, `[[hot]]`, `[[overview]]`. Verified with grep — no remaining `.md`-suffixed wikilinks in `log.md`.

### [empty-sections] 5 content-page headings have no body — **stale at scan time**

Re-check at 19:45 finds all five sections already populated:

- `wiki/topics/Data Preprocessing.md` `## Current Understanding` — 4 subsections (fit-on-training-only, `fit_transform` shortcut, impact on supervised learning, impact on unsupervised learning).
- `wiki/topics/Feature Engineering.md` `## Current Understanding` — 6 subsections (representation-dominates-algorithm, categorical encoding, continuous discretisation, interactions/polynomials, nonlinear transforms, expert knowledge).
- `wiki/topics/Feature Selection.md` `## Current Understanding` — 4 subsections (three families, benchmark numbers, supervised nature, real-world gains).
- `wiki/topics/Unsupervised Learning.md` `## Key Concepts` — 4 populated subsections; `## Current Understanding` — 2 populated subsections.

These were filled as part of the 2026-04-21 Chapter 3 ingest and the 2026-04-22 Chapter 4 extension. The scan was looking at a section-header index that treated `###` subsections as non-content. **No action taken.**

### `thin-pages` / `seed` status (informational, not a finding)

19 pages still carry `status: seed`, all from the 2026-04-17 Transformer ingest. The 30-day rule kicks in on 2026-05-17; none qualify today. For visibility: [[Beam Search]], [[BLEU Score]], [[Byte-Pair Encoding]], [[Label Smoothing]], [[Layer Normalization]], [[Residual Connection]], [[Aidan N. Gomez]], [[Ashish Vaswani]], [[Google Brain]], [[Google Research]], [[Illia Polosukhin]], [[Jakob Uszkoreit]], [[Llion Jones]], [[Niki Parmar]], [[Noam Shazeer]], [[tensor2tensor]], [[University of Toronto]], [[WMT 2014]], [[Łukasz Kaiser]].

## Coverage Suggestions

### Questions the wiki cannot answer well yet

1. **How do classical-ML dimensionality reduction ([[Principal Component Analysis]], [[Non-Negative Matrix Factorization]]) relate to neural-network representation learning?** Both subgraphs exist in isolation with no bridging page.
2. **What is the Transformer's direct lineage?** The wiki cites [[Attention Is All You Need]] but has no Bahdanau 2014 (additive attention), Sutskever 2014 (seq2seq), Luong 2015 (global/local attention), or Lin et al. 2017 (intra-sentence attention) pages — the paper's own "Related Work" section is un-ingested.
3. **How does the Transformer compare to ByteNet, ConvS2S, and RNN-based NMT on the same WMT 2014 benchmark?** The [[Transformer]] page mentions all three; only the Transformer row has numbers.
4. **Which supervised learning algorithms precede the feature-engineering workflow in Müller & Guido?** Chapters 1–2 (linear models, decision trees, ensembles, SVMs, k-NN) are un-ingested, so [[Feature Engineering]] cannot yet point at the *specific* classifiers it enables.
5. **How do train-test pipelines, cross-validation, and grid search — the scaffolding around every claim on this wiki — actually work in `sklearn.model_selection`?** Chapters 5–6 (pipelines, evaluation, grid search) are un-ingested.

### Sources that would strengthen coverage

1. **A representation-learning bridge paper** — Hinton & Salakhutdinov (2006) "Reducing the dimensionality of data with neural networks", or Bengio et al. (2013) "Representation Learning: A Review and New Perspectives" — connects [[Principal Component Analysis]] / [[t-SNE]] to deep representations.
2. **Transformer predecessors** — Bahdanau, Cho, Bengio (2014) "Neural machine translation by jointly learning to align and translate", and Sutskever, Vinyals, Le (2014) "Sequence to sequence learning with neural networks". Would turn the Transformer paper's `## Background` section from plain text into a linked subgraph.
3. **Müller & Guido Chapters 1–2 and 5–7** — the missing supervised-learning and pipelines material. Would raise this book's coverage from 2 chapters to all 7 and retire five `[!gap]` callouts currently on the source page.

## Resolution Pass — 2026-04-24 19:45

Actionable fixes applied this session:

- **HIGH #2 (14 pages):** Appended `## Sources` footer to 13 entity pages ([[Aidan N. Gomez]], [[Ashish Vaswani]], [[Google Brain]], [[Google Research]], [[Illia Polosukhin]], [[Jakob Uszkoreit]], [[Llion Jones]], [[Niki Parmar]], [[Noam Shazeer]], [[tensor2tensor]], [[University of Toronto]], [[WMT 2014]], [[Łukasz Kaiser]]), each listing `[[Attention Is All You Need]]`. Renamed `## Raw Source` → `## Sources` on [[Attention Is All You Need]] itself.
- **LOW #1 (log.md):** Stripped `.md` suffix from three wikilinks (`[[index]]`, `[[hot]]`, `[[overview]]`).

Verified stale at re-check (no action needed):

- **HIGH #1 (manifest):** Already contains 3 active entries for the Müller & Guido book.
- **HIGH #3 (ByteNet):** No `[[ByteNet]]` wikilink exists anywhere; only plain-text mentions.
- **LOW #2 (empty sections):** All 5 sections already populated via the 2026-04-21 / 2026-04-22 ingests.

Post-pass state: **0 findings outstanding.**

## Sources

- `.claude/skills/wiki-lint.md` — 15-check matrix and output contract.
- `raw/manifest.yaml` — source-lifecycle ground truth.
